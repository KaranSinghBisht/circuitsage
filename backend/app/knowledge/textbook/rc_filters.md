# RC Filter Notes

For a first-order RC low-pass filter, cutoff frequency is fc = 1/(2*pi*R*C). A test frequency below cutoff should not be heavily attenuated. Unexpected attenuation at a low frequency often points to the wrong capacitor value, swapped input/output nodes, or an open capacitor ground return.
